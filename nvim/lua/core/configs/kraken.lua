require("kraken").setup({
    commands = {
        locate_tests = {
            open_command = "vsp", -- command used to open the test file, e.g. "e", "split", "lefta vsp"
        },
        add_test_file = {
            open_command = "vsp", -- command used to open the test file, e.g. "e", "split", "lefta vsp"
        },
    },
})
